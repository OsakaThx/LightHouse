<%- include('../partials/header', { title: 'Error 500 - Error del Servidor' }) %>

<div class="min-h-screen bg-gray-100 flex flex-col justify-center items-center px-4">
    <div class="max-w-md w-full bg-white p-8 rounded-lg shadow-md text-center">
        <div class="text-6xl text-red-500 mb-4">
            <i class="fas fa-exclamation-triangle"></i>
        </div>
        <h1 class="text-3xl font-bold text-gray-800 mb-4">500 - Error del Servidor</h1>
        <p class="text-gray-600 mb-6">
            Lo sentimos, algo salió mal en nuestro servidor. Por favor, inténtalo de nuevo más tarde.
        </p>
        <% if (typeof error !== 'undefined') { %>
            <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-6 rounded">
                <p class="font-semibold">Detalles del error:</p>
                <p class="text-sm mt-1"><%= error.message || 'Error desconocido' %></p>
                <% if (process.env.NODE_ENV === 'development' && error.stack) { %>
                    <pre class="bg-gray-100 p-2 mt-2 text-xs overflow-x-auto rounded"><code><%= error.stack %></code></pre>
                <% } %>
            </div>
        <% } else { %>
            <div class="bg-yellow-100 border-l-4 border-yellow-500 text-yellow-700 p-4 mb-6 rounded">
                <p>No se proporcionaron detalles adicionales sobre el error.</p>
            </div>
        <% } %>
        <a 
            href="/" 
            class="inline-block bg-amber-600 hover:bg-amber-700 text-white font-bold py-2 px-6 rounded-md transition duration-300"
        >
            Volver al inicio
        </a>
    </div>
</div>

<%- include('../partials/footer') %>
